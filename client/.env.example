# Copy this file to client/.env.local and fill in real values.

# Base URL of the deployed server (Express + Socket.IO). Read at build time
# by Next.js and inlined into the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:3000
